kind = "web"
previewPath = "/"
title = "Zain Kuwait Payment Portal"
version = "1.0.0"
id = "artifacts/payment-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25089

[services.development]
run = "pnpm --filter @workspace/payment-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/payment-portal", "run", "build" ]
publicDir = "artifacts/payment-portal/.output/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25089"
BASE_PATH = "/"
